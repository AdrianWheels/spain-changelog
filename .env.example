# Copia a .env y rellena con los valores reales (npx @insforge/cli current / secrets).
# Server-only: sin prefijo PUBLIC_, nunca llegan al navegador.
INSFORGE_URL=https://<appkey>.<region>.insforge.app
INSFORGE_API_KEY=ik_xxxxxxxxxxxxxxxxxxxxxxxx
